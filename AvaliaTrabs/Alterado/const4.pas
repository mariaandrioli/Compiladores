program const3 ;
const max = 100;
   min =  max div 2;
var i, j: integer ;

function soma(var a,b : integer):integer;
const inutil = 100;
   begin
      soma:=inutil;
   end;


begin
   i:=min;
   j:=soma(i,max);(* deve indicar erro *)
end.

